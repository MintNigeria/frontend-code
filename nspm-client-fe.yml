version: '3.3'
services:
  myapp:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKER_REGISTRY}nspm-client-fe:latest
    container_name: nspm-client-fe
    restart: always
    ports:
      - "5000:80"
