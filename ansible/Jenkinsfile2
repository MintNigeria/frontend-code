pipeline {
    agent any

    stages {
        stage('Deploy with Ansible') {
            environment {
                ANSIBLE_HOST = 'localhost'
                ANSIBLE_USER = 'VAC_Admin'
                ANSIBLE_PRIVATE_KEY = credentials('ANSIBLE_PRIVATE_KEY')
            }
            steps {
                sh 'ansible-playbook -i ${ANSIBLE_HOST}, --user ${ANSIBLE_USER} --private-key ${ANSIBLE_PRIVATE_KEY} ansible/playbook.yml'
            }
        }

        stage('Build and Start Docker Containers') {
            steps {
                sh 'docker-compose -f /var/lib/jenkins/workspace/ngmintclient/ansible/docker-compose.yml up -d --force-recreate'
            }
        }
    }
}
